import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Bosscho.lab08',  // ตั้งชื่อไม่ซ้ำใคร
  appName: 'lab08-ai',
  webDir: 'dist'
};

export default config;